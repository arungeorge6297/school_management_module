<?xml version="1.0" encoding="UTF-8" ?>

<odoo>
     <record id="hss_model_action" model="ir.actions.act_window">
        <field name="name">student Detail</field>
        <field name="res_model">studenttable.model</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="context">{}</field>
        <field name="domain">[('class_number', '&gt;', 10), ('class_number', '&lt;=', 12)]</field>
    </record>
    <menuitem id="hss_model_action"
              name="Higher Secondary school Students"
              parent="student_menu_root"
              action="hss_model_action"
              sequence="5"/>


</odoo>