<?xml version="1.0" encoding="UTF-8" ?>

<odoo>
     <record id="hs_model_action" model="ir.actions.act_window">
        <field name="name">student Detail</field>
        <field name="res_model">studenttable.model</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="context">{}</field>
        <field name="domain">[('class_number', '&gt;', 7), ('class_number', '&lt;=', 10)]</field>
    </record>
    <menuitem id="hs_model_action"
              name="High School Students"
              parent="student_menu_root"
              action="hs_model_action"
              sequence="5"/>


</odoo>